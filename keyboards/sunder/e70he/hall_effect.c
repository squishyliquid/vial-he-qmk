#include "hall_effect.h"

hall_effect_t key_settings = {
    .mode = RAPID_TRIGGER_MODE,
    .sensitivity = SENSITIVITY,
    .travel_distance = TRAVEL_DISTANCE,
    .actuation_point = ACTUATION_POINT
};